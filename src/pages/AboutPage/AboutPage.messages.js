/*
 * AboutPage Messages
 *
 * This contains all the text for the AboutPage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.pages.AboutPage';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'AboutPage title!',
  },
});
